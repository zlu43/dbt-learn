version: 2

sources:
  - name: stripe
    database: workspace
    schema: default
    tables:
      - name: payments
        freshness: 
          error_after:
            count: 2
            period: day
          warn_after:
            count: 1
            period: day
      